# business agent overlay

Read `../../os/AGENTS.md` first and follow the boot chain in `../../os/me.md`.

Then read this repository's `readme.md`, `status.md`, and `knowledge-map.md`. Replace this generic overlay during onboarding with confirmed business purpose, boundaries, source hierarchy, protected foundations, and wrap rules.

The business owns all of its documents and implementation source in this one repository. Never create a nested repository or submodule. Shared rules come from `../../os/`; personal context comes from `../../life/` only when the task genuinely needs it.
