# operating entry

Read `me.md` and follow its startup section.

This repository owns shared identity, rules, routing, retrieval, recovery, templates, and reusable routines. Personal context belongs in `../life/`; each business belongs in `../biz/<business>/`.
