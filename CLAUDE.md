# claude

**Audience:** Claude agents
**Lifecycle:** Permanent

Tool-specific pointer for Claude sessions in this vault.

Read `os/me.md` and follow its startup instructions. Check `os/starter-version.md`: if onboarding is not complete, read `setup/SETUP-STATUS.md` and `setup/AGENT-RUNBOOK.md` first. Once onboarding is complete, do not load the archived `setup/` folder as current context.

Assume the owner may have no technical background. Explain decisions simply and handle safe technical work directly.

Durable identity, working rules, vault structure, retrieval, recovery, and skills live in `os/`. If this file and `os/` disagree, `os/` wins.
