# vault agent entry

Read `os/AGENTS.md` first. It is the canonical boot source for the whole vault.

Then follow the nearest repository-level `AGENTS.md` when working inside `life/` or a business. Local files may add boundaries; they may not weaken the shared operating rules.

During onboarding only, also follow `setup/SETUP-STATUS.md` and `setup/AGENT-RUNBOOK.md`.
